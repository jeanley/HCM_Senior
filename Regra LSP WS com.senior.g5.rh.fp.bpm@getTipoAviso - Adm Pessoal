DEFINIR ALFA    CTIPAVI;
DEFINIR ALFA    ASQL;
DEFINIR ALFA    AAVIPRE;
DEFINIR ALFA  ACODAVI;

ASQL= "SELECT KEYNAM,VALKEY FROM R996LSF WHERE lstnam = 'LAviPre'";                                


  SQL_CRIAR(CTIPAVI);
  SQL_USARABRANGENCIA(CTIPAVI,0);
  SQL_USARSQLSENIOR2(CTIPAVI,0);
  SQL_DEFINIRCOMANDO(CTIPAVI,ASQL);
  SQL_ABRIRCURSOR(CTIPAVI);
  ENQUANTO(SQL_EOF(CTIPAVI) = 0)
  {
    SQL_RETORNARALFA(CTIPAVI,"VALKEY",AAVIPRE);
  	SQL_RETORNARALFA(CTIPAVI,"KEYNAM",ACODAVI);
  	
  	@ CRIANDO LINHA @
  	getTipoAviso.motivos.CRIARLINHA();
  	getTipoAviso.motivos.codavi = ACODAVI;
  	getTipoAviso.motivos.desavi = AAVIPRE;
  	
  	SQL_PROXIMO(CTIPAVI);
  }
  SQL_FECHARCURSOR(CTIPAVI);
  SQL_DESTRUIR(CTIPAVI);
